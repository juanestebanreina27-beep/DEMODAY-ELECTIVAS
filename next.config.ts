import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // En Next.js 15/16, el host suele manejarse automáticamente en el proxy o
  // se puede configurar de forma más específica si es necesario.
  // Por ahora, dejamos la configuración limpia ya que el servidor arrancó.
};

export default nextConfig;
